"""claude_gateway: self-hosted HTTP gateway exposing the claude_ai SDK.

Public factories (`create_fastapi_app` / `create_litestar_app` /
`create_aiohttp_app`) and `run.main` are re-exported here in a later task; for now
this package only carries the base contracts and shared infra.
"""
